@model Customer

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@Html.Partial("_Form", Model)

@Html.Partial("~/Views/Shared/_DateStamp.cshtml", Model)

@Html.Action("Changes", new { id = Model.Id })

<p><a href="@Url.Action("Index")">Back to List</a></p>